#include<bits/stdc++.h>

using namespace std;

int a[110],b[110];
int ans[110];

int main()
{
    int T,m,n;
    printf("------> Input Time: ");
    scanf("%d",&T);
    while(T--)
    {
        printf("------> Input data of m, n: ");
        // scanf("%d %d", &m, &n);
        cin >> m >> n;
        printf("======> Output data of m, n: ", m, n);
    }
    printf("--------------Finish--------------\n");
    return 0;
}